import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // We can add remotePatterns or other configurations if needed later
};

export default nextConfig;
